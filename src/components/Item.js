const Item = () => {

    return (

        <h3>Soy el Item</h3>
        // Este componente va a renderizar los datos que le lleguen por PROPS
        // Renderizo el ID
        // Renderizo el username
        // Renderizo el mail
        // Renderizo la ciudad
    )

}

export default Item